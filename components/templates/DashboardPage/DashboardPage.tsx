import styles from '@/styles/dashboardPage/dashboardPage.module.scss'

const DashboardPage = () => {
  // const [newProduct, ]
  return (
    <section className={styles.dashboard}>
      <div className={`container ${styles.dashboard__container}`}>

      </div>
    </section>
  )
}

export default DashboardPage
